apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arc.fullname" . }}-test-connection"
  labels:
    {{- include "arc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
